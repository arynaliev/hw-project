const SubmitButton = () => {
  return (
    <button
      style={{
        padding: "10px 20px",
        borderRadius: "5px",
        border: "none",
        margin: "10px",
      }}
    >
      Submit
    </button>
  );
};

export default SubmitButton;
